# agent-server 架构图

```
┌─────────────────────────────────────────────────────────────────────┐
│                        React Client (前端)                          │
│                   package/client (Vite + MapBox)                    │
└──────────┬──────────────────────────────────┬───────────────────────┘
           │ POST /api/chat                   │ GET /api/results/:id/*
           │ (SSE streaming)                  │ (layered access)
           ▼                                  ▼
┌─────────────────────────────────────────────────────────────────────┐
│                 agent-server  (Express 5)                           │
│                 package/agent-server                                │
│                                                                     │
│  ┌─────────────┐  ┌──────────────────────────────────────────────┐  │
│  │  index.ts   │  │              app.ts (路由层)                  │  │
│  │  启动编排    │  │                                              │  │
│  │             │  │  GET  /healthz ─────────── 健康检查           │  │
│  │ • loadConfig│  │                                              │  │
│  │ • loadPrompt│  │  POST /api/chat ────────── Chat 模块         │  │
│  │ • createLLM │  │                            │                 │  │
│  │ • createMCP │  │  GET  /api/results/:id/    │                 │  │
│  │ • createApp │  │    ├── summary             │                 │  │
│  │ • shutdown  │  │    ├── geojson             │                 │  │
│  └─────────────┘  │    ├── triples             │                 │  │
│                   │    ├── grids/region.png     │                 │  │
│                   │    └── grids/:index.png     │                 │  │
│                   └──────────────┬──────────────┘                │  │
│                                  │                                   │
│  ┌───────────────────────────────┼──────────────────────────────┐   │
│  │                   Chat 模块 (核心流程)                        │   │
│  │                                                              │   │
│  │   ┌──────────┐    ┌──────────────┐    ┌───────────────────┐  │   │
│  │   │ Zod 校验  │───▶│ AI SDK       │───▶│ SSE 流式响应      │  │   │
│  │   │ Request   │    │ streamText() │    │ pipeUIMessage...  │  │   │
│  │   └──────────┘    │ maxSteps: 8  │    └───────────────────┘  │   │
│  │                   └──────┬───────┘                           │   │
│  │                          │ tools                             │   │
│  │              ┌───────────┴───────────┐                       │   │
│  │              ▼                       ▼                       │   │
│  │   ┌──────────────────┐   ┌────────────────────┐              │   │
│  │   │ MCP Tools (动态)  │   │ to_results_id      │              │   │
│  │   │ • parse_geo_text  │   │ (内置辅助工具)      │              │   │
│  │   │ • compute_fuzzy.. │   │ 提取结果 ID         │              │   │
│  │   └──────────────────┘   └────────────────────┘              │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │                 Results API (安全层)                          │   │
│  │                                                              │   │
│  │   isSafeId()  ──▶  resolveResultsDir()  ──▶  readMetaJson() │   │
│  │       │                    │                       │         │   │
│  │   路径遍历检查        symlink 解析            读取 meta.json  │   │
│  │   (无 .. / \)       allowedDir 边界                          │   │
│  └──────────────────────────────────────────────────────────────┘   │
│                                                                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────┐  │
│  │  config.ts   │  │  prompt.ts   │  │       mcp.ts             │  │
│  │  Zod Schema  │  │  系统提示词   │  │  MCP Client + Schemas   │  │
│  │  配置校验     │  │  (中文)缓存   │  │  computeFuzzyLocation   │  │
│  └──────────────┘  └──────────────┘  └──────────────────────────┘  │
└────────────┬────────────────────────────────┬───────────────────────┘
             │                                │
             ▼                                ▼
┌────────────────────────┐      ┌──────────────────────────────────┐
│   DeepSeek LLM API     │      │   MCP Server (HTTP /mcp)         │
│   @ai-sdk/deepseek     │      │   package/mcp-server             │
│                        │      │                                  │
│  • deepseek-chat 模型   │      │  ┌────────────────────────────┐  │
│  • 中文地理描述理解      │      │  │ Tools:                     │  │
│  • 空间三元组生成        │      │  │  • parse_geo_text          │  │
│                        │      │  │  • compute_fuzzy_location   │  │
└────────────────────────┘      │  └─────────────┬──────────────┘  │
                                │                │                  │
                                │                ▼                  │
                                │  ┌────────────────────────────┐  │
                                │  │  @geolocus/core            │  │
                                │  │  空间推理引擎               │  │
                                │  │  • 模糊区域计算             │  │
                                │  │  • 概率网格生成             │  │
                                │  │  • 代表性点提取             │  │
                                │  └─────────────┬──────────────┘  │
                                │                │                  │
                                │                ▼                  │
                                │  ┌────────────────────────────┐  │
                                │  │  temp-files/               │  │
                                │  │  ├── <id>/meta.json        │  │
                                │  │  └── <id>/*.png            │  │
                                │  └────────────────────────────┘  │
                                └──────────────────────────────────┘
```

## 数据流

```
用户输入(中文地理描述)
    │
    ▼
Client ──POST /api/chat──▶ agent-server ──▶ DeepSeek LLM
                                │                │
                                │    ◀── 工具调用 ┘
                                │
                                ▼
                           MCP Server ──▶ Core 空间引擎
                                │              │
                                │    ◀── 结果 ──┘
                                │
                                ▼
                           temp-files/ (meta.json + PNG)
                                │
Client ◀── SSE stream ─────────┘
    │
    ▼
Client ──GET /api/results/:id/*──▶ agent-server ──▶ 读取 temp-files/
    │                                                     │
    ▼                                                     │
MapBox GL 可视化 ◀────────────── JSON / PNG ──────────────┘
```

## 关键设计特点

| 特性 | 实现 |
|------|------|
| **解耦** | agent-server 不直接依赖 `@geolocus/core`，通过 MCP 协议间接调用 |
| **流式响应** | AI SDK `streamText()` + SSE，实时推送 LLM 输出和工具调用结果 |
| **分层结果访问** | 5 个细粒度端点，按需获取 summary / geojson / triples / PNG |
| **安全边界** | 路径遍历防护、symlink 解析、`allowedDir` 白名单、不泄露绝对路径 |
| **配置校验** | Zod schema 严格校验 `config.json`，启动时快速失败 |
| **最多 8 步工具调用** | `stopWhen: stepCountIs(8)` 防止 LLM 无限循环 |
